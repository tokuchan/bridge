# bridge

Bridge the gaps between C++ standards, compilers, and STL implementations.
Header-only libraries: rivets (detection macros), truss (polyfills), deck
(higher-level utilities built on truss).
